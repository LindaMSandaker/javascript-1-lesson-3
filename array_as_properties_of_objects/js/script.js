// Create a pet Object

const dog = {
    breeds: ["Labrador", "Pow-pow", "Golden retriever"],
};
console.log(dog.breeds);

for (let i = 0; dog.length; i++) {
    console.log(dog[i].breeds);
}

// JSON JavaScript Object Notation
// airBnB Network Fetch/XHR
// elephant-api
// postman.com